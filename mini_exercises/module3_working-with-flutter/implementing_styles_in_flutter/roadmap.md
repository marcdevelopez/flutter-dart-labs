# Roadmap

- Paso 1: Crear estructura del proyecto y un `main.dart` minimal. ✅ Estructura base y `StyleLabApp` con tema aplicado.
- Paso 2: Añadir fuente a `assets/fonts`, actualizar `pubspec.yaml`, ejecutar `flutter pub get`. ✅ Fuente custom declarada y cargada.
- Paso 3: Definir `ThemeData` (colores, `TextTheme`, `ElevatedButtonTheme`). ✅ Paleta seed/secondary, `TextTheme` y botón global configurados.
- Paso 4: Maquetar la pantalla con `Scaffold`, `AppBar`, `ListView` de tarjetas. ✅ Layout con header y 3 tarjetas mock.
- Paso 5: Estilizar `Text` local en título principal y en las tarjetas. ✅ Copys con estilos locales (headline, body, title/description).
- Paso 6: Personalizar `ElevatedButton` (color, `padding`, `textStyle`). ✅ Ajustado overlay con alpha para compatibilidad CI.
- Paso 7: Decorar `Container` (bordes redondeados, sombra, gradiente opcional) para que las tarjetas destaquen sobre el fondo. ✅ Cards con fondo blanco, esquinas redondeadas y sombra suave.
- Paso 8: Revisión visual rápida (`flutter run`), ajustar colores/tamaños. ✅ Verificado en run; overlay de botones visible y cards contrastando.
